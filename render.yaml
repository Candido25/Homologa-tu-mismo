services:
  - type: web
    name: homologa-tu-mismo
    runtime: node
    plan: free
    branch: main
    autoDeployTrigger: commit
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 22.18.0
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
